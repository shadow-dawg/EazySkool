<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View Marks</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/stylesheets/student.css">
</head>
<body class="bg-gray-900 text-gray-100 flex justify-center items-center min-h-screen">

    <div class="container bg-gray-800 p-8 rounded-lg shadow-lg max-w-4xl w-full">
        <!-- Title -->
        <h1 class="text-3xl font-bold text-center mb-6 text-blue-400">View Marks</h1>

        <!-- Marks Table -->
        <div class="overflow-x-auto shadow-lg rounded-lg">
            <table class="min-w-full bg-gray-700 text-gray-100 border-collapse rounded-lg">
                <thead>
                    <tr class="text-left border-b-2 border-gray-600">
                        <th class="px-6 py-4 text-lg font-semibold">Subject</th>
                        <th class="px-6 py-4 text-lg font-semibold">Marks</th>
                    </tr>
                </thead>
                <tbody>
                    <!-- Science Marks -->
                    <tr class="border-b border-gray-600 hover:bg-gray-600">
                        <td class="px-6 py-3 text-xl font-semibold">Science</td>
                        <td class="px-6 py-3 text-lg"><%= marks.science || 'Not Available' %></td>
                    </tr>
                    <!-- Maths Marks -->
                    <tr class="border-b border-gray-600 hover:bg-gray-600">
                        <td class="px-6 py-3 text-xl font-semibold">Maths</td>
                        <td class="px-6 py-3 text-lg"><%= marks.maths || 'Not Available' %></td>
                    </tr>
                    <!-- Social Science Marks -->
                    <tr class="border-b border-gray-600 hover:bg-gray-600">
                        <td class="px-6 py-3 text-xl font-semibold">Social Science</td>
                        <td class="px-6 py-3 text-lg"><%= marks.socialscience || 'Not Available' %></td>
                    </tr>
                    <!-- English Marks -->
                    <tr class="border-b border-gray-600 hover:bg-gray-600">
                        <td class="px-6 py-3 text-xl font-semibold">English</td>
                        <td class="px-6 py-3 text-lg"><%= marks.english || 'Not Available' %></td>
                    </tr>
                </tbody>
            </table>
        </div>

        <!-- Back Button -->
        <div class="flex justify-center mt-6">
            <a href="/teacher-dashboard/students" class="bg-blue-600 hover:bg-blue-700 text-white px-6 py-2 rounded-md shadow-lg">Back to Dashboard</a>
        </div>
    </div>

</body>
</html>
