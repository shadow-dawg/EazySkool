<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Student Dashboard</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 text-gray-200 font-sans">

  <!-- Dashboard Container -->
  <div class="min-h-screen flex items-center justify-center">
    <div class="w-full max-w-5xl bg-gray-800 shadow-lg rounded-lg overflow-hidden">

      <!-- Header Section -->
      <header class="p-6 bg-gray-700 flex justify-between items-center">
        <h1 class="text-2xl font-semibold">Welcome, <%= user.username %> 👋</h1>
        <button class="bg-red-500 px-4 py-2 rounded-md text-white hover:bg-red-600">
          <a href="/logout">Logout</a>
        </button>
      </header>

      <!-- Main Content -->
      <main class="flex flex-col lg:flex-row">
        
        <!-- Left Section: User Data -->
        <div class="w-full lg:w-2/3 p-8">
          <h2 class="text-xl font-semibold mb-4">Your Information</h2>
          <div class="space-y-4">
            <% if (studentData) { %>
              <p><span class="font-bold text-gray-300">Name:</span> <%= studentData.name %></p>
              <p><span class="font-bold text-gray-300">Class:</span> <%= studentData.Class %></p>
              <p><span class="font-bold text-gray-300">Roll No:</span> <%= studentData.rollno %></p>
              <p><span class="font-bold text-gray-300">Phone No:</span> <%= studentData.phone %></p>
              <p><span class="font-bold text-gray-300">Address:</span> <%= studentData.address %></p>
            <% } else { %>
              <p><span class="font-bold text-gray-300">Name:</span> <%= user.username %></p>
              <p><span class="font-bold text-gray-300">Email:</span> <%= user.email %></p>
              <p><span class="font-bold text-gray-300">Role:</span> <%= user.role %></p>
              <p class="text-yellow-400">No additional student information available. Please contact your teacher.</p>
            <% } %>
          </div>

          <!-- Marks Section -->
          <% if (subjectData) { %>
            <h2 class="text-xl font-semibold mb-4 mt-8">Your Marks</h2>
            <table class="min-w-full bg-gray-700 rounded-lg overflow-hidden">
              <thead>
                <tr>
                  <th class="px-6 py-3 text-left text-gray-400">Subject</th>
                  <th class="px-6 py-3 text-left text-gray-400">Marks</th>
                </tr>
              </thead>
              <tbody>
                <tr>
                  <td class="px-6 py-3 text-gray-300">Science</td>
                  <td class="px-6 py-3 text-gray-300"><%= subjectData.science || 'N/A' %></td>
                </tr>
                <tr>
                  <td class="px-6 py-3 text-gray-300">Maths</td>
                  <td class="px-6 py-3 text-gray-300"><%= subjectData.maths || 'N/A' %></td>
                </tr>
                <tr>
                  <td class="px-6 py-3 text-gray-300">Social Science</td>
                  <td class="px-6 py-3 text-gray-300"><%= subjectData.socialscience || 'N/A' %></td>
                </tr>
                <tr>
                  <td class="px-6 py-3 text-gray-300">English</td>
                  <td class="px-6 py-3 text-gray-300"><%= subjectData.english || 'N/A' %></td>
                </tr>
                <tr>
                  <td class="px-6 py-3 text-gray-300 font-bold">Total Marks</td>
                  <td class="px-6 py-3 text-gray-300 font-bold"><%= subjectData.TotalMarks || 'N/A' %></td>
                </tr>
              </tbody>
            </table>
          <% } else { %>
            <p class="text-yellow-400 mt-4">No marks available for this student. Please contact your teacher.</p>
          <% } %>
        </div>

        <!-- Right Section: Profile Picture -->
        <div class="w-full lg:w-1/3 bg-gray-900 flex items-center justify-center p-8">
          <div class="w-48 h-48 rounded-full overflow-hidden shadow-md">
            <% if (studentData && studentData.profilePicture) { %>
              <img class="w-full h-full object-cover" src="/images/uploads/<%= studentData.profilePicture %>" alt="Profile Picture">
            <% } else { %>
              <img class="w-full h-full object-cover" src="/images/uploads/default.png" alt="Default Profile Picture">
            <% } %>                  
          </div>
        </div>
      </main>
    </div>
  </div>
</body>
</html>
